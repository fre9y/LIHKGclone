export interface User {
    id: number;
    nickname: string;
    email: string,
    is_admin: boolean,
    is_p: boolean,
    is_male: boolean,
    show: boolean,
    created_at: Date,
    updated_at: Date,
}
//not using currently